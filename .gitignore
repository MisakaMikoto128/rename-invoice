# 审计日志（运行时生成，不入库）
rename_invoice.log

# 静默模式队列与锁（运行时生成，不入库）
.queue.txt
.queue.lock
.leader.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 虚拟环境
.venv/
venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# 测试样本（可能含敏感发票信息，禁止提交）
samples/
test_invoices/
*.pdf

# Rust build artifacts (apps/cli/rust = 程序1 的 Rust 绿色版实现)
apps/cli/rust/target/
# pdfium.dll 由 release zip 单独分发, 不进 git (7 MB 二进制)
apps/cli/rust/pdfium.dll
apps/cli/rust/release/
apps/cli/rust/.queue.*

# explorer-extension (程序3) 构建产物
apps/explorer-extension/target/
apps/explorer-extension/package/*.dll
apps/explorer-extension/package/*.exe
apps/explorer-extension/package/*.msix
apps/explorer-extension/.sign/
*.pfx
*.xlsx

# 单元测试用的脱敏 PDF 样本, 本地放, 不入库
tests/_pdf_samples/

# PyInstaller / flet pack 产物
*.spec

.nuitka_cache/

# build.py 输出 + 自签名证书
release/
build_sign.pfx
apps/account-manager/.sign/
